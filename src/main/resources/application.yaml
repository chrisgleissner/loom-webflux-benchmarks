spring:
  application:
    name: loom-webflux
  main:
    banner-mode: off
  threads:
    virtual:
      enabled: true

server:
  http2:
    enabled: true

management:
  endpoints:
    web:
      exposure:
        include: "heapdump, shutdown, threaddump"
  endpoint:
    shutdown:
      enabled: true
